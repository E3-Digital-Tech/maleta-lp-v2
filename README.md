# A Maleta do Advogado, versão curta

Segunda versão da LP de vendas da Maleta (E3 Digital): herói, o que tem dentro, planos com as travas que cada tamanho resolve e os entregáveis com ícone.

`index.html` é gerado por `build-v2.py` a partir do `index.html` do repo `maleta-lp` (a versão completa). Screenshots: `python -m http.server 5411` e `node shot.mjs 5411`.
